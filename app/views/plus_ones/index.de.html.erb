<div class="container">
  <div class="row">
    <div class="offset-lg-2 col-lg-8">
      <%= render partial: 'guests/leader' %>
      <%= render partial: 'guests/rsvp_nav', locals: { active: 2 } %>

      <h2>Gäste</h2>
      <p>Gib uns bitte Bescheid, ob du einen Gast mitbringen wirst. Kinder und Hunde sind auch herzlich eingeladen.
      <p>&nbsp;</p>

      <% if @plus_ones.any? %>
        <%= render @plus_ones %>
      <% else %>
        <p>Noch keine Gäste hinzugefügt.</p>
      <% end %>
      <p>
        <%= link_to 'Add Plus One', new_guest_plus_one_path(@guest), class: 'btn btn-primary', data: { disable_with: 'Loading...' } %>
      </p>

      <p>&nbsp;</p>
      <p class="guests-buttons">
        <%= link_to 'Continue', more_info_guest_path(@guest), class: 'btn btn-primary' %>
      </p>
    </div>
  </div>
</div>
