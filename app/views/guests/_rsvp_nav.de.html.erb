<nav class="guests-rsvp-nav">
  <ul class="nav nav-pills nav-fill">
    <li class="nav-item">
      <%= link_to '1. Über dich', guest_path(@guest), class: ['nav-link', active == 1 ? 'active' : ''] %>
    </li>
    <li class="nav-item">
      <% if active >= 2 %>
        <%= link_to '2. Gäste', guest_plus_ones_path(@guest), class: ['nav-link', active == 2 ? 'active' : ''] %>
      <% else %>
        <a class="nav-link <%= active == 2 ? 'active' : '' %>">2. Gäste</a>
      <% end %>
    </li>
    <li class="nav-item">
      <% if active >= 3 %>
        <%= link_to '3. Mehr Info', more_info_guest_path(@guest), class: ['nav-link', active == 3 ? 'active' : ''] %>
      <% else %>
        <a class="nav-link <%= active == 3 ? 'active' : '' %>">3. Mehr Info</a>
      <% end %>
    </li>
    <li class="nav-item">
      <% if active >= 4 %>
        <%= link_to '4. Bestätigen', confirm_guest_path(@guest), class: ['nav-link', active == 4 ? 'active' : ''] %>
      <% else %>
        <a class="nav-link <%= active == 4 ? 'active' : '' %>">4. Bestätigen</a>
      <% end %>
    </li>
  </ul>
</nav>
